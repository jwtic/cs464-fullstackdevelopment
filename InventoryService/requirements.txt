# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.1

# --- Database & ORM ---
SQLAlchemy==2.0.31
alembic==1.13.2
# Postgres driver (optional; InventoryService defaults to SQLite).
# psycopg2-binary often fails to install on Python 3.13 without wheels.
psycopg2-binary==2.9.10; python_version < "3.13"
# For SQLite in local testing, no extra package needed.

# --- Authentication & Security ---
bcrypt==4.0.1
python-jose[cryptography]==3.3.0   # JWT encoding/decoding
passlib[bcrypt]==1.7.4             # Password hashing
pydantic[email]==2.9.2             # Email validation

# --- Testing & Development ---
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.2                      # Async HTTP client for integration tests
requests==2.32.3                   # Optional, often used for service-to-service calls

# --- Image processing / Azure CV helper ---
Pillow==10.4.0
python-dotenv==1.0.1
python-multipart==0.0.20
